# Remap-Offline-Printers---IP-Only

This script was built to increase the speed at which offline printers could be remapped at work due to them randomly showing up as offline for a large number of computers, despite the printers themselves still being reachable at their Static IPs.
